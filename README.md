# SQL-versus-Pandas
Mini project to show variuos SQL style operations with pandas (a python library)

# Aim
The aim of this project is to demonstrate the equivalence between SQL queries and pandas data operations given the background I have in SQL.

# Tech Stack
SQL
Python and Pandas
Jupyter Notebooks

# Dataset 
Heart Disease:
https://www.kaggle.com/datasets/oktayrdeki/heart-disease/data)
Heart Attack Risk Prediction:
https://www.kaggle.com/datasets/iamsouravbanerjee/heart-attack-prediction-dataset 


## Covered Topics
| SQL Concept | pandas Equivalent |
|-------------|-------------------|
| `SELECT`    | `df[]`            |
| `LIMIT`     | `head()`          |
| `WHERE`     | Boolean filtering |
| `ORDER BY`  | `sort_values()`   |
| `JOIN`      | `merge()`         |
| `GROUP BY`  | `groupby()`       |
| `CASE WHEN` | `np.where()` / `apply()` |
